/**
 * The test verifies the correctness of the implementation of string-buffer.
 * There are two threads and first one sends random strings into second one by string-buffer.
 * The test is passed if the input data is the same as the output data.
 * Otherwise program will be aborted. 
 */

void string_buffer_test();