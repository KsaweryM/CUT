/**
 * The test verifies the correctness of the implementation of integer-buffer.
 * There are two threads and first one sends integers into second one by integer-buffer.
 * The test is passed if the input data is the same as the output data.
 * Otherwise program will be aborted. 
 */

void integer_buffer_test();